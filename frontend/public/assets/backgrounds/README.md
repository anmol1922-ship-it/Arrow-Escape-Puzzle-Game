# Arrow Escape Assets

Keep visual assets grouped by purpose: backgrounds, game objects, particles,
and audio. Assets must support the same restrained Arrow Escape visual language,
remain optional for core play, and have a reduced-motion or no-audio fallback.

Avoid large unoptimized assets, unrelated illustration styles, and decorative
content that competes with the board.
