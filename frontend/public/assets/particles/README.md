# Particle Assets

Particles are optional reward feedback only. Keep them lightweight, decorative,
and removable when reduced motion is enabled. Never use a particle effect as
the only signal for a successful move, blocked move, or completion.
