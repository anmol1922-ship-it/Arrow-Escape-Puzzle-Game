# Game Assets

Store reusable puzzle-object artwork here. Prefer CSS or inline SVG for small
directional objects so the board remains crisp at every viewport. Game assets
must not contain rule logic or require a network request to render a level.
